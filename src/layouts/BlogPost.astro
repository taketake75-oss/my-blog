---
import type { CollectionEntry } from 'astro:content';
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';

type Props = CollectionEntry<'blog'>['data'];

const { title, description, pubDate, updatedDate, heroImage } = Astro.props;

// サイドバー用に全記事を取得して最新順に並べ替え
const allPosts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<html lang="ja">
	<head>
		<BaseHead title={title} description={description} />
		<style>
			/* 全体コンテナ：スマホ表示時もはみ出さないレスポンシブ設計 */
			.container {
				width: 100%;
				max-width: 1200px;
				margin: 0 auto;
				padding: 1.5rem 1rem;
				box-sizing: border-box;
				display: grid;
				grid-template-columns: 1fr;
				gap: 2rem;
			}
			@media (min-width: 900px) {
				.container {
					grid-template-columns: 1fr 300px;
					padding: 2rem 1rem;
				}
			}

			/* 本文エリア */
			.main-content {
				width: 100%;
				min-width: 0; /* flex/grid内の文字溢れ防止 */
			}
			.title-section {
				margin-bottom: 1.5em;
				padding-bottom: 1em;
				border-bottom: 1px solid #e2e8f0;
				text-align: center;
			}
			.title-section h1 {
				font-size: 1.5rem;
				line-height: 1.4;
				margin: 0.5em 0;
				word-break: break-word; /* 長い単語の折り返し */
			}
			@media (min-width: 768px) {
				.title-section h1 {
					font-size: 2rem;
				}
			}
			.date {
				color: rgb(var(--gray));
				font-size: 0.85rem;
			}

			/* 文章・マークダウン本文（商品カードや画像のはみ出しを防止） */
			.prose {
				width: 100%;
				max-width: 100%;
				overflow-wrap: break-word;
			}
			/* 埋め込まれたHTMLカードや画像のレスポンシブ強制適用 */
			.prose :global(img) {
				max-width: 100% !important;
				height: auto !important;
			}
			.prose :global(div) {
				max-width: 100% !important;
				box-sizing: border-box !important;
			}

			/* サイドバー */
			.sidebar {
				width: 100%;
				display: flex;
				flex-direction: column;
				gap: 1.5rem;
			}
			.widget {
				background: #f8fafc;
				border: 1px solid #e2e8f0;
				border-radius: 12px;
				padding: 1.25rem;
			}
			.widget-title {
				font-size: 1.1rem;
				font-weight: bold;
				margin-top: 0;
				margin-bottom: 1rem;
				padding-bottom: 0.5rem;
				border-bottom: 2px solid rgb(var(--accent));
			}
			.post-list {
				list-style: none;
				padding: 0;
				margin: 0;
			}
			.post-list li {
				margin-bottom: 0.8rem;
				padding-bottom: 0.8rem;
				border-bottom: 1px dashed #cbd5e1;
			}
			.post-list li:last-child {
				margin-bottom: 0;
				padding-bottom: 0;
				border-bottom: none;
			}
			.post-list a {
				text-decoration: none;
				color: rgb(var(--black));
				font-size: 0.9rem;
				line-height: 1.4;
				display: block;
			}
			.post-list a:hover {
				color: rgb(var(--accent));
			}
			.post-date {
				font-size: 0.75rem;
				color: rgb(var(--gray));
				display: block;
				margin-top: 0.2rem;
			}
		</style>
	</head>

	<body>
		<Header />
		<div class="container">
			<!-- 左側：記事本文エリア -->
			<main class="main-content">
				<article>
					<div class="title-section">
						<div class="date">
							<FormattedDate date={pubDate} />
							{
								updatedDate && (
									<div class="last-updated-on">
										更新日: <FormattedDate date={updatedDate} />
									</div>
								)
							}
						</div>
						<h1>{title}</h1>
					</div>
					{heroImage && <div class="hero-image"><img src={heroImage} alt="" /></div>}
					<div class="prose">
						<slot />
					</div>
				</article>
			</main>

			<!-- 右側：サイドバーエリア -->
			<aside class="sidebar">
				<div class="widget">
					<h3 class="widget-title">最新・おすすめ記事</h3>
					<ul class="post-list">
						{
							allPosts.map((post) => (
								<li>
									<a href={`/blog/${post.id}/`}>
										{post.data.title}
										<span class="post-date">
											<FormattedDate date={post.data.pubDate} />
										</span>
									</a>
								</li>
							))
						}
					</ul>
				</div>
			</aside>
		</div>
		<Footer />
	</body>
</html>